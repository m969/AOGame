import "csharp";
import "puerts";
import ptypeof = puer.$typeof;
import ppromise = puer.$promise;
import fgui = CS.FairyGUI;
import ET = CS.ET;
import AO = CS.AO;
import UI_LobbyWindowObject from "../../auto_generates/Login/UI_LobbyWindowObject.mjs";

export default class UI_LobbyWindow extends UI_LobbyWindowObject {
    constructor(GObject: fgui.GObject) {
        super(GObject);
    }

    onOpen() {
        super.onOpen();
    }

    onClose() {
        super.onClose();
    }
}
