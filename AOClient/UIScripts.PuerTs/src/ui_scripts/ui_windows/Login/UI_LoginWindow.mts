import "csharp";
import "puerts";
import ptypeof = puer.$typeof;
import ppromise = puer.$promise;
import fgui = CS.FairyGUI;
import ET = CS.ET;
import AO = CS.AO;
import UI_LoginWindowObject from "../../auto_generates/Login/UI_LoginWindowObject.mjs";

export default class UI_LoginWindow extends UI_LoginWindowObject {
    constructor(GObject: fgui.GObject) {
        super(GObject);
    }

    onOpen() {
        super.onOpen();
    }

    onClose() {
        super.onClose();
    }
}
